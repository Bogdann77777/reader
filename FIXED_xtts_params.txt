# ИСПРАВЛЕННЫЕ ПАРАМЕТРЫ XTTS
# 
# Найди в app.py функцию api_synthesize (примерно строка 493-506)
# И замени параметры tts_model.tts_to_file на эти:

# СТАРЫЕ ПАРАМЕТРЫ (вызывают артефакты):
"""
tts_model.tts_to_file(
    text=text_stripped,
    speaker_wav=voice_path_abs,
    language=language,
    file_path=str(output_path),
    speed=speed,
    temperature=0.9,  # ← СЛИШКОМ ВЫСОКАЯ = галлюцинации
    length_penalty=1.0,
    repetition_penalty=5.0  # ← СЛИШКОМ ВЫСОКАЯ = добавляет случайные звуки
)
"""

# НОВЫЕ ПАРАМЕТРЫ (без артефактов):
tts_model.tts_to_file(
    text=text_stripped,
    speaker_wav=voice_path_abs,
    language=language,
    file_path=str(output_path),
    speed=speed,
    temperature=0.65,  # Понижено с 0.9 → меньше "креативности"
    length_penalty=1.0,
    repetition_penalty=2.0  # Понижено с 5.0 → не будет добавлять лишние звуки
)
